# 사이트 기능 전체 정리

## 📋 개요

**kwonjae-2.github.io**는 Jekyll 기반의 개인 기술 블로그 사이트로, 개발자를 위한 주간 기술 아티클 큐레이션 기능을 제공합니다. GitHub Pages를 통해 자동 배포되며, AI 기반 자동화 시스템을 통해 매주 새로운 콘텐츠를 생성합니다.

---

## 🎯 핵심 기능

### 1. **주간 기술 아티클 큐레이션 (Weekly Digest)**

#### 1.1 자동 수집 시스템
- **RSS 피드 수집**: 다양한 기술 블로그와 커뮤니티에서 아티클 자동 수집
  - SRE Weekly
  - GeekNews (하드웨어 개발자 뉴스)
  - OpenTelemetry Blog
  - Grafana Blog
  - Honeycomb Blog

#### 1.2 AI 기반 큐레이션
- **Claude AI (claude-sonnet-4-20250514)** 활용
- **자동 선별**: 수집된 아티클 중 개발자에게 가치 있는 3-10개 선별
- **선별 기준**:
  - 개발자 성장에 도움
  - 실용적 가치 (실무 적용 가능)
  - 현대적 개발 관행과의 관련성
  - 혁신성 (새로운 도구, 기법, 관점)
  - 생산성/코드 품질/시스템 안정성 향상

#### 1.3 이중 언어 지원
- **한국어/영어 요약**: 각 아티클에 대해 양국어 요약 및 핵심 포인트 제공
- **구조화된 포맷**: 
  - 아티클 제목 및 출처
  - 2-3문장 요약
  - 3가지 핵심 포인트

#### 1.4 중복 방지
- 이전에 게시된 아티클 URL 자동 추적
- 중복 게시 방지

---

### 2. **자동화 워크플로우**

#### 2.1 GitHub Actions 자동화
- **스케줄**: 매주 금요일 오후 9시 KST (UTC 12:00) 실행
- **수동 실행**: `workflow_dispatch`를 통한 수동 트리거 지원
- **자동 커밋**: 생성된 포스트를 자동으로 커밋 및 푸시
- **아티팩트 백업**: 생성된 파일을 30일간 보관

#### 2.2 배포 프로세스
- **자동 배포**: `gh-pages` 브랜치에 푸시 시 GitHub Pages 자동 배포
- **발행 시점**: 매주 토요일 오전 10시 KST

---

### 3. **사이트 구조 및 페이지**

#### 3.1 메인 페이지 (`index.html`)
- 홈페이지 레이아웃
- 프로필 정보 표시

#### 3.2 주간 페이지 (`/weekly`)
- 모든 주간 포스트 목록 표시
- Week 번호 및 날짜 표시
- 아티클 개수 표시
- 최신순 정렬

#### 3.3 태그 페이지 (`/tags`)
- 모든 태그 목록 표시
- 태그별 포스트 그룹화
- 스무스 스크롤 네비게이션
- 태그 클릭 시 해당 섹션으로 자동 스크롤

#### 3.4 포스트 페이지 (`/posts/*`)
- 개별 포스트 상세 보기
- 읽기 시간 표시 (`read-time` 기능)
- 태그 표시 및 링크
- 이전/다음 포스트 네비게이션
- 관련 포스트 추천
- 작성자 정보 블록
- Disqus 댓글 시스템 (weekly 카테고리 포함)

---

### 4. **UI/UX 기능**

#### 4.1 반응형 디자인
- 모든 디바이스에서 최적화된 레이아웃
- 모바일, 태블릿, 데스크톱 지원

#### 4.2 다크 테마 자동 전환
- **시스템 설정 연동**: 사용자의 OS 다크/라이트 모드 설정 자동 감지
- **자동 전환**: `dark-theme: auto` 설정으로 시스템 설정에 따라 자동 변경

#### 4.3 애니메이션
- 부드러운 페이지 전환 효과
- 설정 가능 (`animation: true`)

#### 4.4 소셜 링크
- LinkedIn 프로필 링크
- GitHub 프로필 링크
- 이메일 링크
- **Insights Hub**: 전구 아이콘을 통한 Scribbles Issues 링크

---

### 5. **콘텐츠 관리 기능**

#### 5.1 포스트 구조
- **Front Matter**: Jekyll 표준 메타데이터
  - 제목, 날짜, 카테고리, 태그
  - 작성자 정보
  - 설명 (description)
  - 아티클 개수 (`article_count`)

#### 5.2 카테고리 시스템
- `weekly`: 주간 큐레이션 포스트
- 카테고리별 필터링 및 네비게이션

#### 5.3 태그 시스템
- 다중 태그 지원
- 태그별 포스트 그룹화
- 태그 페이지에서 통합 관리

#### 5.4 숨김 기능
- `hidden: true` 설정으로 특정 포스트 숨김 가능
- 기본값: `hidden: false`

---

### 6. **SEO 및 최적화**

#### 6.1 SEO 플러그인
- `jekyll-seo-tag`: 자동 SEO 메타 태그 생성
- `jekyll-feed`: RSS 피드 자동 생성 (`/feed.xml`)

#### 6.2 성능 최적화
- HTML 압축 (`jekyll-compress-html`)
- 개발 환경 제외 압축

#### 6.3 접근성
- ARIA 레이블 지원
- 시맨틱 HTML 구조
- 키보드 네비게이션 지원

---

### 7. **기술 스택**

#### 7.1 사이트 프레임워크
- **Jekyll**: 정적 사이트 생성기
- **GitHub Pages**: 호스팅 및 배포
- **SCSS/SASS**: 스타일시트 전처리기

#### 7.2 자동화 도구
- **Python 3.11**: 아티클 수집 스크립트
- **feedparser**: RSS 피드 파싱
- **Anthropic Claude API**: AI 큐레이션 및 요약

#### 7.3 플러그인
- `jekyll-seo-tag`: SEO 최적화
- `jekyll-gist`: GitHub Gist 통합
- `jekyll-feed`: RSS 피드 생성
- `jemoji`: 이모지 지원

---

### 8. **데이터 저장 및 백업**

#### 8.1 포스트 저장
- `_posts/` 디렉토리에 마크다운 파일로 저장
- 파일명 형식: `YYYY-MM-DD-weekly-YYYYMMDD.md`

#### 8.2 수집 데이터 백업
- `scripts/output/` 디렉토리에 JSON 형식으로 저장
- 파일명 형식: `articles_YYYYMMDD.json`
- GitHub Actions 아티팩트로 30일간 보관

---

### 9. **설정 및 커스터마이징**

#### 9.1 사이트 설정 (`_config.yml`)
- 사이트 제목, 설명, 프로필 정보
- 소셜 링크 설정
- 테마 설정 (다크/라이트/자동)
- 플러그인 활성화/비활성화
- 레이아웃 옵션 (너비: normal/large)

#### 9.2 기능 토글
- `weekly`: 주간 페이지 활성화
- `read-time`: 읽기 시간 표시
- `show-tags`: 태그 표시
- `related`: 관련 포스트 표시
- `show-author`: 작성자 정보 표시
- `animation`: 애니메이션 활성화

---

### 10. **보안 및 개인정보**

#### 10.1 API 키 관리
- GitHub Secrets를 통한 안전한 API 키 저장
- 환경 변수로 API 키 주입

#### 10.2 외부 링크
- `rel="noopener"` 속성으로 보안 강화
- 외부 링크에 `target="_blank"` 적용

---

## 📊 데이터 흐름

```
1. RSS 피드 수집 (Python 스크립트)
   ↓
2. Claude AI 분석 및 선별 (3-10개 아티클)
   ↓
3. 이중 언어 요약 생성 (한국어/영어)
   ↓
4. 마크다운 포스트 생성
   ↓
5. GitHub Actions 자동 커밋/푸시
   ↓
6. GitHub Pages 자동 배포
   ↓
7. 사이트에 게시 (토요일 오전 10시 KST)
```

---

## 🔧 개발 및 유지보수

### 로컬 개발 환경
- Jekyll 로컬 서버 실행 가능
- Docker Compose 지원 (`docker-compose.yml`)
- Gemfile을 통한 의존성 관리

### 스크립트 실행
- `scripts/collect_articles.py`: 수동 실행 가능
- 환경 변수 `ANTHROPIC_API_KEY` 필요

### 파일 구조
- `_posts/`: 블로그 포스트
- `_layouts/`: 페이지 레이아웃
- `_includes/`: 재사용 가능한 컴포넌트
- `_sass/`: 스타일시트
- `assets/`: 이미지 및 정적 파일
- `scripts/`: 자동화 스크립트

---

## 📈 향후 확장 가능성

1. **추가 소스 통합**: 더 많은 RSS 피드 추가 가능
2. **카테고리 확장**: weekly 외 다른 카테고리 추가 가능
3. **검색 기능**: 사이트 내 검색 기능 추가 가능
4. **뉴스레터**: 이메일 구독 기능 추가 가능
5. **통계 대시보드**: 아티클 조회수, 인기 태그 등 분석

---

## 📝 요약

이 사이트는 **자동화된 기술 아티클 큐레이션 플랫폼**으로, 다음과 같은 핵심 가치를 제공합니다:

1. ✅ **자동화**: 수동 작업 없이 매주 새로운 콘텐츠 생성
2. ✅ **AI 큐레이션**: 고품질 아티클 선별 및 요약
3. ✅ **이중 언어**: 한국어/영어 동시 지원
4. ✅ **개발자 중심**: 실무에 도움이 되는 콘텐츠 선별
5. ✅ **현대적 UX**: 반응형 디자인, 다크 테마, 빠른 로딩
6. ✅ **SEO 최적화**: 검색 엔진 친화적 구조
7. ✅ **확장 가능**: 모듈화된 구조로 쉽게 기능 추가 가능


